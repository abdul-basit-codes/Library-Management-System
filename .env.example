# Library Management System — environment reference
# Copy to .env (or .env.local) and fill in values. All are optional on Vercel (no env needed for in-memory mode).

# Set to "true" to enable structured request logging
LOG_REQUESTS=true

# Database / persistence (coming soon)
DB_NAME=lms_db
# DB_CONNECTION_STRING=